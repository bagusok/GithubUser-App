<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bagusok.github">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cok"
        tools:targetApi="31">
        <activity
            android:name=".ui.main.setting.SettingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.favorite.FavoriteUserActivity"
            android:exported="false" />
        <activity
            android:name="com.bagusok.github.ui.main.DetailUserActivity"
            android:exported="false" />
        <activity
            android:name="com.bagusok.github.ui.main.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.splash.SplashScreenActivity"
            android:theme="@style/SplashScreen"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>